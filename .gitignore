.DS_Store
node_modules/
*.log
.env

# Test coverage output
backend/coverage/

# Trivy SARIF reports (generated in CI, not needed in repo)
trivy-*.sarif

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.tflock
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Sensitive tfvars — never commit secrets
terraform/environments/dev/terraform.tfvars.local
terraform/environments/staging/terraform.tfvars.local
terraform/environments/prod/terraform.tfvars.local
