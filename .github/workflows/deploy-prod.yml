name: Deploy — Production

# Stages: Lint → Test → Build → SonarCloud → Trivy → Manual Approval → Deploy
# Plan runs in parallel from Lint so it is ready before the approval gate.

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm production deployment'
        required: true

permissions:
  contents: read
  security-events: write
  pull-requests: read

concurrency:
  group: deploy-prod
  cancel-in-progress: false

env:
  ENVIRONMENT:                    prod
  TF_DIR:                         terraform/environments/prod
  AWS_REGION:                     ${{ secrets.AWS_REGION }}
  TF_VAR_aws_region:              ${{ secrets.AWS_REGION }}
  TF_VAR_db_password:             ${{ secrets.DB_PASSWORD }}
  TF_VAR_grafana_admin_password:  ${{ secrets.GRAFANA_ADMIN_PASSWORD }}

jobs:

  # ── Stage 1: Lint ─────────────────────────────────────────────────────────
  lint:
    name: "Stage 1 • Lint"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        working-directory: backend
        run: npm install
      - name: Run ESLint
        working-directory: backend
        run: npm run lint

  # ── Stage 2: Test ─────────────────────────────────────────────────────────
  test:
    name: "Stage 2 • Test"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        working-directory: backend
        run: npm install
      - name: Syntax check
        working-directory: backend
        run: node --check server.js
      - name: Run tests
        working-directory: backend
        run: npm test

  # ── Stage 3: Build ────────────────────────────────────────────────────────
  # Builds the Docker image only after tests pass, then pushes to ECR with an
  # immutable SHA tag. The tag is reused by Trivy and Deploy — no rebuild.
  build:
    name: "Stage 3 • Build"
    runs-on: ubuntu-latest
    needs: test
    outputs:
      ecr_url: ${{ steps.push.outputs.ecr_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}
      - name: Build and push Docker image to ECR
        id: push
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_REPO="sample-app-${ENVIRONMENT}-backend"
          ECR_REGISTRY="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          ECR_URL="${ECR_REGISTRY}/${ECR_REPO}"
          aws ecr describe-repositories --repository-names "${ECR_REPO}" \
            --region "${AWS_REGION}" > /dev/null 2>&1 || \
          aws ecr create-repository --repository-name "${ECR_REPO}" \
            --image-tag-mutability IMMUTABLE \
            --image-scanning-configuration scanOnPush=true \
            --region "${AWS_REGION}" > /dev/null
          aws ecr get-login-password --region "$AWS_REGION" | \
            docker login --username AWS --password-stdin "$ECR_REGISTRY"
          docker build -t sample-backend ./backend
          docker tag sample-backend:latest "$ECR_URL:${GITHUB_SHA}"
          docker push "$ECR_URL:${GITHUB_SHA}"
          echo "ecr_url=$ECR_URL" >> $GITHUB_OUTPUT

  # ── Terraform Plan (parallel track) ──────────────────────────────────────
  # Starts after Lint — runs alongside Test/Build so it is ready at the gate.
  plan:
    name: "Terraform Plan"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}
      - name: Terraform init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="encrypt=true"
      - name: Clear stale state lock
        continue-on-error: true
        run: aws s3 rm "s3://${{ secrets.TF_STATE_BUCKET }}/prod/terraform.tfstate.tflock" || true
      - name: Terraform validate
        working-directory: ${{ env.TF_DIR }}
        run: terraform validate
      - name: Terraform plan
        working-directory: ${{ env.TF_DIR }}
        run: terraform plan -no-color -lock=false -out=tfplan
      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-prod
          path: ${{ env.TF_DIR }}/tfplan
          retention-days: 1

  # ── Stage 4: SonarCloud ───────────────────────────────────────────────────
  # Code quality and security analysis. Runs after Build succeeds so that a
  # broken build never reaches the quality gate.
  sonarcloud:
    name: "Stage 4 • SonarCloud"
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        working-directory: backend
        run: npm install
      - name: Run tests with coverage
        working-directory: backend
        run: npm run test:coverage
      - name: SonarCloud analysis
        uses: SonarSource/sonarcloud-github-action@v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN:  ${{ secrets.SONAR_TOKEN }}

  # ── Stage 5: Trivy Security Scan ──────────────────────────────────────────
  # Three scans in one stage: filesystem (secrets/CVEs), IaC (Terraform
  # misconfigs), and the container image already in ECR from Stage 3.
  trivy:
    name: "Stage 5 • Trivy"
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - name: Trivy — Filesystem & Secrets
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: sarif
          output: trivy-fs.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          scanners: vuln,secret,misconfig
          skip-dirs: node_modules,.terraform,nginx
          skip-files: docker-compose.yml,.env.example
          trivyignores: .trivyignore

      - name: Upload filesystem results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-fs.sarif
          category: trivy-filesystem

      - name: Trivy — IaC debug (show findings)
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: terraform/
          format: table
          severity: CRITICAL,HIGH
          exit-code: '0'
          trivyignores: .trivyignore

      - name: Trivy — Filesystem debug (show findings)
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          format: table
          severity: CRITICAL,HIGH
          exit-code: '0'
          scanners: misconfig
          skip-dirs: node_modules,.terraform,nginx
          trivyignores: .trivyignore

      - name: Trivy — IaC (Terraform)
        if: always()
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: config
          scan-ref: terraform/
          format: sarif
          output: trivy-iac.sarif
          severity: CRITICAL,HIGH
          exit-code: '0'
          trivyignores: .trivyignore

      - name: Upload IaC results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-iac.sarif
          category: trivy-iac

      - name: Configure AWS credentials for ECR pull
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}

      - name: Pull image from ECR
        id: ecr_pull
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_REGISTRY="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          ECR_URL="${ECR_REGISTRY}/sample-app-${ENVIRONMENT}-backend"
          aws ecr get-login-password --region "$AWS_REGION" | \
            docker login --username AWS --password-stdin "$ECR_REGISTRY"
          docker pull "$ECR_URL:${GITHUB_SHA}"
          echo "image_ref=$ECR_URL:${GITHUB_SHA}" >> $GITHUB_OUTPUT

      - name: Trivy — Container Image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: image
          image-ref: ${{ steps.ecr_pull.outputs.image_ref }}
          format: sarif
          output: trivy-image.sarif
          severity: CRITICAL,HIGH
          exit-code: '1'
          ignore-unfixed: true
          trivyignores: .trivyignore

      - name: Upload image results
        uses: github/codeql-action/upload-sarif@v3
        if: always() && hashFiles('trivy-image.sarif') != ''
        with:
          sarif_file: trivy-image.sarif
          category: trivy-container

  # ── Stage 6: Manual Approval ──────────────────────────────────────────────
  # SonarCloud, Trivy, AND Terraform Plan must all pass before this gate.
  # workflow_dispatch: type "deploy" to confirm. push to main: auto-approved.
  approve:
    name: "Stage 6 • Manual Approval"
    runs-on: ubuntu-latest
    needs: [sonarcloud, trivy, plan]
    steps:
      - name: Verify manual confirmation
        if: github.event_name == 'workflow_dispatch'
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "deploy" ]; then
            echo "Deployment cancelled — type 'deploy' to confirm."
            exit 1
          fi
          echo "Approved by ${{ github.actor }}. Deploying to production..."

      - name: Auto-approved on push to main
        if: github.event_name == 'push'
        run: echo "Push to main by ${{ github.actor }}. Proceeding to prod deploy."

  # ── Stage 7: Deploy ───────────────────────────────────────────────────────
  # Applies Terraform, updates the ECS task definition to the SHA-tagged image
  # built in Stage 3 (no second build), then deploys the frontend to S3.
  deploy:
    name: "Stage 7 • Deploy"
    runs-on: ubuntu-latest
    needs: [approve, build]
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}
      - name: Terraform init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="encrypt=true"
      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-prod
          path: ${{ env.TF_DIR }}
      - name: Terraform apply
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve -lock=false tfplan

      - name: Capture Terraform outputs
        id: tf_outputs
        working-directory: ${{ env.TF_DIR }}
        run: |
          echo "ecs_cluster=$(terraform output -raw ecs_cluster_name)"         >> $GITHUB_OUTPUT
          echo "ecs_service=$(terraform output -raw ecs_service_name)"         >> $GITHUB_OUTPUT
          echo "backend_url=$(terraform output -raw backend_url)"              >> $GITHUB_OUTPUT
          echo "frontend_bucket=$(terraform output -raw frontend_bucket_name)" >> $GITHUB_OUTPUT
          echo "frontend_url=$(terraform output -raw frontend_url)"            >> $GITHUB_OUTPUT

      - name: Resolve deployment variables
        id: vars
        env:
          TF_ECS_CLUSTER:     ${{ steps.tf_outputs.outputs.ecs_cluster }}
          TF_ECS_SERVICE:     ${{ steps.tf_outputs.outputs.ecs_service }}
          TF_BACKEND_URL:     ${{ steps.tf_outputs.outputs.backend_url }}
          TF_FRONTEND_BUCKET: ${{ steps.tf_outputs.outputs.frontend_bucket }}
          TF_FRONTEND_URL:    ${{ steps.tf_outputs.outputs.frontend_url }}
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          ECR_URL="${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/sample-app-${ENVIRONMENT}-backend"
          ECS_CLUSTER=${TF_ECS_CLUSTER:-sample-app-${ENVIRONMENT}-cluster}
          ECS_SERVICE=${TF_ECS_SERVICE:-sample-app-${ENVIRONMENT}-backend-service}
          BACKEND_URL=${TF_BACKEND_URL:-$(aws elbv2 describe-load-balancers \
            --names "sample-app-${ENVIRONMENT}-alb" \
            --query 'LoadBalancers[0].DNSName' --output text)}
          FRONTEND_BUCKET=${TF_FRONTEND_BUCKET:-$(aws s3api list-buckets \
            --query "Buckets[?contains(Name,\`sample-app-${ENVIRONMENT}-frontend\`)].Name" \
            --output text | awk '{print $1}')}
          FRONTEND_URL=${TF_FRONTEND_URL:-"http://${FRONTEND_BUCKET}.s3-website-${AWS_REGION}.amazonaws.com"}
          echo "ecr_url=$ECR_URL"                >> $GITHUB_OUTPUT
          echo "ecs_cluster=$ECS_CLUSTER"         >> $GITHUB_OUTPUT
          echo "ecs_service=$ECS_SERVICE"         >> $GITHUB_OUTPUT
          echo "backend_url=$BACKEND_URL"         >> $GITHUB_OUTPUT
          echo "frontend_bucket=$FRONTEND_BUCKET" >> $GITHUB_OUTPUT
          echo "frontend_url=$FRONTEND_URL"       >> $GITHUB_OUTPUT

      - name: Update ECS task definition and deploy
        env:
          ECS_CLUSTER: ${{ steps.vars.outputs.ecs_cluster }}
          ECS_SERVICE:  ${{ steps.vars.outputs.ecs_service }}
          ECR_URL:      ${{ steps.vars.outputs.ecr_url }}
        run: |
          FULL_IMAGE="$ECR_URL:${GITHUB_SHA}"
          TASK_FAMILY=$(aws ecs describe-services \
            --cluster "$ECS_CLUSTER" --services "$ECS_SERVICE" \
            --query 'services[0].taskDefinition' --output text | \
            sed 's|.*/||' | cut -d: -f1)
          TASK_DEF=$(aws ecs describe-task-definition \
            --task-definition "$TASK_FAMILY" --query 'taskDefinition' --output json)
          NEW_TASK_DEF=$(echo "$TASK_DEF" | jq \
            --arg IMAGE "$FULL_IMAGE" '
            .containerDefinitions[0].image = $IMAGE |
            del(.taskDefinitionArn,.revision,.status,.requiresAttributes,.compatibilities,.registeredAt,.registeredBy)')
          NEW_TASK_ARN=$(aws ecs register-task-definition \
            --cli-input-json "$NEW_TASK_DEF" \
            --query 'taskDefinition.taskDefinitionArn' --output text)
          aws ecs update-service \
            --cluster "$ECS_CLUSTER" --service "$ECS_SERVICE" \
            --task-definition "$NEW_TASK_ARN"
          MAX_WAIT=1200
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            STATUS=$(aws ecs describe-services \
              --cluster "$ECS_CLUSTER" --services "$ECS_SERVICE" \
              --query 'services[0].deployments[?status==`PRIMARY`].rolloutState' \
              --output text)
            echo "Deployment rollout: $STATUS (${ELAPSED}s / ${MAX_WAIT}s)"
            if [ "$STATUS" = "COMPLETED" ]; then echo "Deployment succeeded."; break; fi
            if [ "$STATUS" = "FAILED" ]; then
              echo "Deployment FAILED. Stopped task diagnostics:"
              STOPPED=$(aws ecs list-tasks --cluster "$ECS_CLUSTER" \
                --service-name "$ECS_SERVICE" --desired-status STOPPED \
                --max-items 3 --query 'taskArns[]' --output text)
              [ -n "$STOPPED" ] && aws ecs describe-tasks \
                --cluster "$ECS_CLUSTER" --tasks $STOPPED \
                --query 'tasks[].{stopCode:stopCode,stoppedReason:stoppedReason,containers:containers[].{name:name,reason:reason,exitCode:exitCode}}'
              exit 1
            fi
            sleep 30
            ELAPSED=$((ELAPSED + 30))
          done
          if [ $ELAPSED -ge $MAX_WAIT ]; then
            echo "Timed out after ${MAX_WAIT}s. Service state:"
            aws ecs describe-services --cluster "$ECS_CLUSTER" --services "$ECS_SERVICE" \
              --query 'services[0].{desired:desiredCount,running:runningCount,pending:pendingCount,deployments:deployments[].{status:status,rollout:rolloutState,desired:desiredCount,running:runningCount,failed:failedTasks}}'
            exit 1
          fi

      - name: Deploy frontend to S3
        env:
          BACKEND_URL:     ${{ steps.vars.outputs.backend_url }}
          FRONTEND_BUCKET: ${{ steps.vars.outputs.frontend_bucket }}
        run: |
          sed -i "s|REPLACE_WITH_BACKEND_API_URL|http://$BACKEND_URL|g" frontend/app.js
          aws s3 sync ./frontend "s3://$FRONTEND_BUCKET" --delete

      - name: Deployment summary
        env:
          FRONTEND_URL: ${{ steps.vars.outputs.frontend_url }}
          BACKEND_URL:  ${{ steps.vars.outputs.backend_url }}
        run: |
          echo "========================================"
          echo "Production deployment complete!"
          echo "Frontend : $FRONTEND_URL"
          echo "Backend  : http://$BACKEND_URL"
          echo "Image SHA: ${GITHUB_SHA}"
          echo "========================================"
