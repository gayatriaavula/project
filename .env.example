# Copy this file to .env and fill in your values for local development.
# docker-compose.yml uses these credentials directly — do not commit .env

DB_HOST=db
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=sampledb
DB_PORT=3306
PORT=3000
