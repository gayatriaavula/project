{
  "name": "sample-backend",
  "version": "1.0.0",
  "description": "Sample backend API for AWS deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "eslint . --ext .js",
    "test": "node --test tests/*.test.js",
    "test:coverage": "c8 --reporter=lcov node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.3.1"
  },
  "devDependencies": {
    "c8": "^10.1.2",
    "eslint": "^8.57.0"
  }
}
